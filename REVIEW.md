# گزارش بازبینی کد — رتبه لند

> **به‌روزرسانی v1.1.0:** تمام موارد P0 و P1 این گزارش پیاده‌سازی و تست شدند.
> جدول‌های زیر با علامت ✅ نشان می‌دهند چه چیزی حل شده است.

## v1.0.2 (بازبینی اولیه)

تاریخ بازبینی: ۲۵ مرداد ۱۴۰۵ · دامنه: کل `app/` (۲۵ ماژول، ~۲٬۹۰۰ خط) + `tools/` + `tests/`
وضعیت تست‌ها: **۲۰۷ passed / 0 failed**

خلاصه در یک خط: **هسته محصول کامل و پایدار است؛ آنچه مانده عمدتاً «حلقه‌های عملیاتی»
است — به‌ویژه نحوهٔ وصل‌شدن دانش‌آموز واقعی به ربات و مسئلهٔ منطقه زمانی.**

---

## ✅ آنچه کامل و تست‌شده است

| حوزه | وضعیت |
|---|---|
| رندر روی قالب رسمی | pixel-accurate، RTL صحیح، دو بک‌اند با fallback دوگانه |
| جریان مشاور | دانش‌آموز → هفته → ۷ روز × ۸ اسلات → تکالیف → پیش‌نمایش → تولید → ارسال |
| Draft و بازیابی | هر ویرایش در دیتابیس؛ مقاوم در برابر ری‌استارت |
| نسخه‌بندی داده | ویرایش پس از تولید ⇒ version+1، رکورد نسخه قبلی در `plan_files` |
| امنیت | IDOR روی ۱۹ اکشن، injection، path traversal، redaction لاگ |
| استقرار | Docker + Railway + Alembic + healthcheck + graceful shutdown |
| مقاومت فایل | کش `file_id` + بازتولید خودکار از دیتابیس |
| ضدقفل‌شدن UI | روتر fallback + تست فشردن تک‌تک دکمه‌ها |

هیچ callback بی‌هندلر، هیچ TODO/FIXME و هیچ کد مرده‌ای در مسیرهای اصلی باقی نمانده
(بررسی خودکار: emitted-vs-handled = صفر اختلاف).

---

## 🔴 P0 — قبل از استفاده واقعی باید حل شود

### ۱. اتصال دانش‌آموز واقعی به ربات ✅ حل شد در v1.1.0

امروز دو مسیر داریم و هیچ‌کدام کامل نیست:

* اگر دانش‌آموز را با `--telegram-id` بسازید: باید عدد آی‌دی او را از قبل بدانید.
* اگر بدون آی‌دی بسازید و دانش‌آموز `/start` بزند: `UserMiddleware` او را با
  `telegram_id` جست‌وجو می‌کند، پیدا نمی‌کند و **یک ردیف کاربر دوم** می‌سازد.
  نتیجه: یک دانش‌آموز، دو رکورد؛ برنامه به رکورد بدون آی‌دی وصل است و ارسال ممکن نیست.

**راه‌حل پیشنهادی (کوچک، ~۱۲۰ خط):** لینک دعوت یک‌بارمصرف.
مشاور در ربات می‌زند «دعوت دانش‌آموز» → لینک `https://t.me/<bot>?start=inv_<token>` →
دانش‌آموز روی آن می‌زند → `/start` با payload، رکورد موجود به `telegram_id` او وصل
می‌شود. جدول `invites` یا ستون `invite_token` روی `users`.

### ۲. منطقه زمانی ✅ حل شد در v1.1.0

`saturday_of(date.today())` از ساعت **سرور** استفاده می‌کند و Railway روی UTC است.
بین ساعت ۰۰:۰۰ تا ۰۳:۳۰ به‌وقت تهران، سرور هنوز «دیروز» است ⇒ «این هفته» و تاریخ
روزها یک روز عقب محاسبه می‌شود. مشاورانی که شب‌ها کار می‌کنند دقیقاً در همین بازه‌اند.

**راه‌حل:** یک `now_tehran()` در `app/domain/persian.py` با
`ZoneInfo("Asia/Tehran")` و جایگزینی سه فراخوانی `date.today()` در
`handlers/advisor.py` و `plan_service.py` + متغیر `TIMEZONE` در env. (~۲۰ خط)

### ۳. افزودن دانش‌آموز از داخل ربات ✅ حل شد در v1.1.0

مشاور برای هر دانش‌آموز جدید باید به Shell رِیل‌وی دسترسی داشته باشد. برای استفاده
روزمره عملی نیست. حداقلِ لازم: در صفحه «👨‍🎓 دانش‌آموزان» دکمه «➕ افزودن دانش‌آموز»
(نام + آی‌دی/لینک دعوت) با دسترسی advisor/admin. (~۱۵۰ خط، با ۱ و ۳ یکجا منطقی است)

---

## 🟠 P1 — باگ‌های کوچک ✅ همگی در v1.1.0 حل شدند

| # | مورد | اثر | تخمین |
|---|---|---|---|
| ۴ | صفحه‌بندی «برنامه‌های قبلی» برای **دانش‌آموز** کار نمی‌کند (`hpage` همیشه با `advisor_id=user.id` کوئری می‌زند) | دانش‌آموز با بیش از ۶ برنامه، صفحه دوم را خالی می‌بیند | ۱۵ خط |
| ۵ | «پیش‌نویس‌ها» صفحه‌بندی ندارد (سقف ۶ مورد) | پیش‌نویس‌های قدیمی‌تر دیده نمی‌شوند | ۲۰ خط |
| ۶ | هنگام صفحه‌بندی roster، عنوان به «انتخاب دانش‌آموز» تغییر می‌کند | ناهماهنگی جزئی UI | ۵ خط |
| ۷ | حذف برنامه، فایل‌های PNG/PDF را از دیسک پاک نمی‌کند + هیچ سیاست نگه‌داری نیست | رشد بی‌پایان Volume (هر برنامه ~۱.۲MB) | ۴۰ خط |
| ۸ | نسخه‌های قبلی (`plan_files`) در UI قابل مشاهده/بازیابی نیستند | داده هست، دسترسی نیست | ۶۰ خط |
| ۹ | `docker-compose.yml` هنوز `TELEGRAM_BOT_TOKEN` را پاس می‌دهد | کار می‌کند (alias) ولی ناهماهنگ با `.env.example` | ۱ خط |
| ۱۰ | خطاهای داخل `DatabaseMiddleware`/`UserMiddleware` توسط `ErrorMiddleware` گرفته نمی‌شوند (ترتیب لایه‌ها) | در قطعی دیتابیس، کاربر پیام فارسی نمی‌گیرد | ۱۰ خط |
| ۱۱ | هر کسی ربات را `/start` کند یک ردیف `users` می‌سازد | نویز دیتابیس (خطر امنیتی ندارد؛ چیزی نمی‌بیند) | ۱۵ خط |

---

## 🟡 P2 — ویژگی‌های اسپک که عمداً ساخته نشدند

| مورد | توضیح |
|---|---|
| فرم مرحله‌ای per-field برای اسلات | فقط Quick Entry (`درس \| مبحث \| کار \| زمان`) داریم؛ اسپک هر دو را می‌خواست |
| کپی تک‌اسلات | «کپی روز» هست، «کپی Slot» نیست |
| ویرایش تک‌تکلیف | الان replace-all (کل تکالیف با یک پیام) |
| پنل ادمین داخل ربات | نسخه قالب، فونت، تنظیمات رندر — فعلاً فقط env/CLI |
| Quick Templates | «برنامه آزمون / جمع‌بندی» آماده |
| Webhook | فعلاً Long Polling (نیازمند دامنه؛ در README به‌عنوان محدودیت ثبت شده) |
| فاز ۳ | AI Planner، Analytics، پیشنهاد هوشمند |

**فیلدهای بلااستفاده در schema:** `users.phone`, `users.grade`,
`activities.notes`, و وضعیت‌های `PlanStatus.READY/ARCHIVED`. یا در UI استفاده شوند یا
در یک migration بعدی حذف شوند (الان صرفاً نویز هستند، ضرری ندارند).

---

## 🔧 کیفیت مهندسی و عملیات

| مورد | وضعیت | پیشنهاد |
|---|---|---|
| Lint / Type check | ندارد | `ruff` + `mypy` در CI (~۳۰ دقیقه کار) |
| Sentry / Alerting | ندارد | خطاهای production فقط در لاگ رِیل‌وی می‌مانند |
| Backup دیتابیس | مستند نشده | Railway → Postgres → Backups را روشن کنید |
| تست بار | ندارد | هر رندر ~۲.۵ ثانیه CPU؛ با `RENDER_CONCURRENCY=2` یعنی سقف ~۴۸ برنامه در دقیقه |
| Coverage report | ندارد | تست‌ها خوب‌اند ولی عدد پوشش گزارش نمی‌شود |
| i18n / چنداُنستیتویی | تک‌مؤسسه، تک‌زبان | فعلاً درست است؛ برای فروش محصول بعداً لازم می‌شود |

---

## 📌 ترتیب پیشنهادی کار

```
گام ۱  (نیم‌روز)   منطقه زمانی تهران            ← P0-۲   جلوگیری از تاریخ اشتباه
گام ۲  (۱ روز)     دعوت/اتصال دانش‌آموز + افزودن از داخل ربات   ← P0-۱ و P0-۳
گام ۳  (نیم‌روز)   باگ‌های P1: صفحه‌بندی‌ها، حذف فایل، ترتیب middleware
گام ۴  (نیم‌روز)   نگه‌داری فایل‌ها (retention) + مشاهده نسخه‌های قبلی
گام ۵  (اختیاری)   ruff/mypy + Sentry
گام ۶              فاز ۳: AI Planner و Analytics
```

پس از گام ۲، سیستم بدون دسترسی به Shell سرور قابل استفاده روزمره است — این خط
واقعی «آماده بهره‌برداری» است.

---

## نکته پایانی

هیچ‌کدام از موارد بالا مانع کار امروز ربات نیست: می‌توانید همین حالا با ساخت
دانش‌آموز از طریق `tools.manage` (با داشتن Telegram ID دانش‌آموز) کل چرخه را
انجام دهید و خروجی PNG/PDF بگیرید. موارد P0 «شکاف عملیاتی» هستند، نه نقص فنی.


---

# آنچه در v1.1.0 انجام شد

| مورد | راه‌حل پیاده‌شده |
|---|---|
| P0-۱ اتصال دانش‌آموز | لینک دعوت یک‌بارمصرف `?start=inv_<token>`؛ ادغام خودکار رکورد تکراری |
| P0-۲ منطقه زمانی | `TIMEZONE=Asia/Tehran` + `today_local()` در همه محاسبات تاریخ |
| P0-۳ افزودن دانش‌آموز | «👨‍🎓 دانش‌آموزان → ➕ افزودن» با نام و پایه، کارت دانش‌آموز، حذف از فهرست |
| P1-۴ صفحه‌بندی دانش‌آموز | `ListCB(kind=…)` — هر فهرست صفحه‌بندی مخصوص خودش را دارد |
| P1-۵ صفحه‌بندی پیش‌نویس‌ها | `drafts_of(limit, offset)` + `count_drafts_of` |
| P1-۶ عنوان فهرست | حالت `pick` / `card` جدا شد |
| P1-۷ فایل‌های یتیم | حذف برنامه فایل‌ها را هم پاک می‌کند + دستور `manage cleanup` |
| P1-۸ نسخه‌های قبلی | دکمه «🗂 نسخه‌های قبلی» با دریافت PNG/PDF هر نسخه |
| P1-۹ docker-compose | `BOT_TOKEN` هم‌نام با بقیه |
| P1-۱۰ ترتیب middleware | `ErrorMiddleware` بیرونی‌ترین لایه شد |
| P1-۱۱ ثبت‌نام ناخواسته | فقط دعوت‌شده‌ها و ادمین‌های `ADMIN_IDS` در دیتابیس ثبت می‌شوند |
| افزوده | دستور `/id` برای دیدن شناسه تلگرام |

**تست‌ها: ۲۰۷ → ۲۳۶** (۲۹ تست جدید فقط برای همین تغییرات).

## آنچه هنوز باقی است (P2 — عمداً)

فرم مرحله‌ای per-field، کپی تک‌اسلات، ویرایش تک‌تکلیف، پنل ادمین داخل ربات،
Webhook، فاز ۳ (AI Planner / Analytics)، و ابزارهای کیفیت (ruff/mypy/Sentry).
فیلد `users.phone` و `activities.notes` هنوز بلااستفاده‌اند (`grade` اکنون استفاده می‌شود).
