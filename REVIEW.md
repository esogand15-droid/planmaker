# گزارش بازبینی کد — رتبه لند

> **به‌روزرسانی v1.1.0:** تمام موارد P0 و P1 این گزارش پیاده‌سازی و تست شدند.
> جدول‌های زیر با علامت ✅ نشان می‌دهند چه چیزی حل شده است.

## v1.0.2 (بازبینی اولیه)

تاریخ بازبینی: ۲۵ مرداد ۱۴۰۵ · دامنه: کل `app/` (۲۵ ماژول، ~۲٬۹۰۰ خط) + `tools/` + `tests/`
وضعیت تست‌ها: **۲۰۷ passed / 0 failed**

خلاصه در یک خط: **هسته محصول کامل و پایدار است؛ آنچه مانده عمدتاً «حلقه‌های عملیاتی»
است — به‌ویژه نحوهٔ وصل‌شدن دانش‌آموز واقعی به ربات و مسئلهٔ منطقه زمانی.**

---

## ✅ آنچه کامل و تست‌شده است

| حوزه | وضعیت |
|---|---|
| رندر روی قالب رسمی | pixel-accurate، RTL صحیح، دو بک‌اند با fallback دوگانه |
| جریان مشاور | دانش‌آموز → هفته → ۷ روز × ۸ اسلات → تکالیف → پیش‌نمایش → تولید → ارسال |
| Draft و بازیابی | هر ویرایش در دیتابیس؛ مقاوم در برابر ری‌استارت |
| نسخه‌بندی داده | ویرایش پس از تولید ⇒ version+1، رکورد نسخه قبلی در `plan_files` |
| امنیت | IDOR روی ۱۹ اکشن، injection، path traversal، redaction لاگ |
| استقرار | Docker + Railway + Alembic + healthcheck + graceful shutdown |
| مقاومت فایل | کش `file_id` + بازتولید خودکار از دیتابیس |
| ضدقفل‌شدن UI | روتر fallback + تست فشردن تک‌تک دکمه‌ها |

هیچ callback بی‌هندلر، هیچ TODO/FIXME و هیچ کد مرده‌ای در مسیرهای اصلی باقی نمانده
(بررسی خودکار: emitted-vs-handled = صفر اختلاف).

---

## 🔴 P0 — قبل از استفاده واقعی باید حل شود

### ۱. اتصال دانش‌آموز واقعی به ربات ✅ حل شد در v1.1.0

امروز دو مسیر داریم و هیچ‌کدام کامل نیست:

* اگر دانش‌آموز را با `--telegram-id` بسازید: باید عدد آی‌دی او را از قبل بدانید.
* اگر بدون آی‌دی بسازید و دانش‌آموز `/start` بزند: `UserMiddleware` او را با
  `telegram_id` جست‌وجو می‌کند، پیدا نمی‌کند و **یک ردیف کاربر دوم** می‌سازد.
  نتیجه: یک دانش‌آموز، دو رکورد؛ برنامه به رکورد بدون آی‌دی وصل است و ارسال ممکن نیست.

**راه‌حل پیشنهادی (کوچک، ~۱۲۰ خط):** لینک دعوت یک‌بارمصرف.
مشاور در ربات می‌زند «دعوت دانش‌آموز» → لینک `https://t.me/<bot>?start=inv_<token>` →
دانش‌آموز روی آن می‌زند → `/start` با payload، رکورد موجود به `telegram_id` او وصل
می‌شود. جدول `invites` یا ستون `invite_token` روی `users`.

### ۲. منطقه زمانی ✅ حل شد در v1.1.0

`saturday_of(date.today())` از ساعت **سرور** استفاده می‌کند و Railway روی UTC است.
بین ساعت ۰۰:۰۰ تا ۰۳:۳۰ به‌وقت تهران، سرور هنوز «دیروز» است ⇒ «این هفته» و تاریخ
روزها یک روز عقب محاسبه می‌شود. مشاورانی که شب‌ها کار می‌کنند دقیقاً در همین بازه‌اند.

**راه‌حل:** یک `now_tehran()` در `app/domain/persian.py` با
`ZoneInfo("Asia/Tehran")` و جایگزینی سه فراخوانی `date.today()` در
`handlers/advisor.py` و `plan_service.py` + متغیر `TIMEZONE` در env. (~۲۰ خط)

### ۳. افزودن دانش‌آموز از داخل ربات ✅ حل شد در v1.1.0

مشاور برای هر دانش‌آموز جدید باید به Shell رِیل‌وی دسترسی داشته باشد. برای استفاده
روزمره عملی نیست. حداقلِ لازم: در صفحه «👨‍🎓 دانش‌آموزان» دکمه «➕ افزودن دانش‌آموز»
(نام + آی‌دی/لینک دعوت) با دسترسی advisor/admin. (~۱۵۰ خط، با ۱ و ۳ یکجا منطقی است)

---

## 🟠 P1 — باگ‌های کوچک ✅ همگی در v1.1.0 حل شدند

| # | مورد | اثر | تخمین |
|---|---|---|---|
| ۴ | صفحه‌بندی «برنامه‌های قبلی» برای **دانش‌آموز** کار نمی‌کند (`hpage` همیشه با `advisor_id=user.id` کوئری می‌زند) | دانش‌آموز با بیش از ۶ برنامه، صفحه دوم را خالی می‌بیند | ۱۵ خط |
| ۵ | «پیش‌نویس‌ها» صفحه‌بندی ندارد (سقف ۶ مورد) | پیش‌نویس‌های قدیمی‌تر دیده نمی‌شوند | ۲۰ خط |
| ۶ | هنگام صفحه‌بندی roster، عنوان به «انتخاب دانش‌آموز» تغییر می‌کند | ناهماهنگی جزئی UI | ۵ خط |
| ۷ | حذف برنامه، فایل‌های PNG/PDF را از دیسک پاک نمی‌کند + هیچ سیاست نگه‌داری نیست | رشد بی‌پایان Volume (هر برنامه ~۱.۲MB) | ۴۰ خط |
| ۸ | نسخه‌های قبلی (`plan_files`) در UI قابل مشاهده/بازیابی نیستند | داده هست، دسترسی نیست | ۶۰ خط |
| ۹ | `docker-compose.yml` هنوز `TELEGRAM_BOT_TOKEN` را پاس می‌دهد | کار می‌کند (alias) ولی ناهماهنگ با `.env.example` | ۱ خط |
| ۱۰ | خطاهای داخل `DatabaseMiddleware`/`UserMiddleware` توسط `ErrorMiddleware` گرفته نمی‌شوند (ترتیب لایه‌ها) | در قطعی دیتابیس، کاربر پیام فارسی نمی‌گیرد | ۱۰ خط |
| ۱۱ | هر کسی ربات را `/start` کند یک ردیف `users` می‌سازد | نویز دیتابیس (خطر امنیتی ندارد؛ چیزی نمی‌بیند) | ۱۵ خط |

---

## 🟡 P2 — ویژگی‌های اسپک که عمداً ساخته نشدند

| مورد | توضیح |
|---|---|
| فرم مرحله‌ای per-field برای اسلات | فقط Quick Entry (`درس \| مبحث \| کار \| زمان`) داریم؛ اسپک هر دو را می‌خواست |
| کپی تک‌اسلات | «کپی روز» هست، «کپی Slot» نیست |
| ویرایش تک‌تکلیف | الان replace-all (کل تکالیف با یک پیام) |
| پنل ادمین داخل ربات | نسخه قالب، فونت، تنظیمات رندر — فعلاً فقط env/CLI |
| Quick Templates | «برنامه آزمون / جمع‌بندی» آماده |
| Webhook | فعلاً Long Polling (نیازمند دامنه؛ در README به‌عنوان محدودیت ثبت شده) |
| فاز ۳ | AI Planner، Analytics، پیشنهاد هوشمند |

**فیلدهای بلااستفاده در schema:** `users.phone`, `users.grade`,
`activities.notes`, و وضعیت‌های `PlanStatus.READY/ARCHIVED`. یا در UI استفاده شوند یا
در یک migration بعدی حذف شوند (الان صرفاً نویز هستند، ضرری ندارند).

---

## 🔧 کیفیت مهندسی و عملیات

| مورد | وضعیت | پیشنهاد |
|---|---|---|
| Lint / Type check | ندارد | `ruff` + `mypy` در CI (~۳۰ دقیقه کار) |
| Sentry / Alerting | ندارد | خطاهای production فقط در لاگ رِیل‌وی می‌مانند |
| Backup دیتابیس | مستند نشده | Railway → Postgres → Backups را روشن کنید |
| تست بار | ندارد | هر رندر ~۲.۵ ثانیه CPU؛ با `RENDER_CONCURRENCY=2` یعنی سقف ~۴۸ برنامه در دقیقه |
| Coverage report | ندارد | تست‌ها خوب‌اند ولی عدد پوشش گزارش نمی‌شود |
| i18n / چنداُنستیتویی | تک‌مؤسسه، تک‌زبان | فعلاً درست است؛ برای فروش محصول بعداً لازم می‌شود |

---

## 📌 ترتیب پیشنهادی کار

```
گام ۱  (نیم‌روز)   منطقه زمانی تهران            ← P0-۲   جلوگیری از تاریخ اشتباه
گام ۲  (۱ روز)     دعوت/اتصال دانش‌آموز + افزودن از داخل ربات   ← P0-۱ و P0-۳
گام ۳  (نیم‌روز)   باگ‌های P1: صفحه‌بندی‌ها، حذف فایل، ترتیب middleware
گام ۴  (نیم‌روز)   نگه‌داری فایل‌ها (retention) + مشاهده نسخه‌های قبلی
گام ۵  (اختیاری)   ruff/mypy + Sentry
گام ۶              فاز ۳: AI Planner و Analytics
```

پس از گام ۲، سیستم بدون دسترسی به Shell سرور قابل استفاده روزمره است — این خط
واقعی «آماده بهره‌برداری» است.

---

## نکته پایانی

هیچ‌کدام از موارد بالا مانع کار امروز ربات نیست: می‌توانید همین حالا با ساخت
دانش‌آموز از طریق `tools.manage` (با داشتن Telegram ID دانش‌آموز) کل چرخه را
انجام دهید و خروجی PNG/PDF بگیرید. موارد P0 «شکاف عملیاتی» هستند، نه نقص فنی.


---

# آنچه در v1.1.0 انجام شد

| مورد | راه‌حل پیاده‌شده |
|---|---|
| P0-۱ اتصال دانش‌آموز | لینک دعوت یک‌بارمصرف `?start=inv_<token>`؛ ادغام خودکار رکورد تکراری |
| P0-۲ منطقه زمانی | `TIMEZONE=Asia/Tehran` + `today_local()` در همه محاسبات تاریخ |
| P0-۳ افزودن دانش‌آموز | «👨‍🎓 دانش‌آموزان → ➕ افزودن» با نام و پایه، کارت دانش‌آموز، حذف از فهرست |
| P1-۴ صفحه‌بندی دانش‌آموز | `ListCB(kind=…)` — هر فهرست صفحه‌بندی مخصوص خودش را دارد |
| P1-۵ صفحه‌بندی پیش‌نویس‌ها | `drafts_of(limit, offset)` + `count_drafts_of` |
| P1-۶ عنوان فهرست | حالت `pick` / `card` جدا شد |
| P1-۷ فایل‌های یتیم | حذف برنامه فایل‌ها را هم پاک می‌کند + دستور `manage cleanup` |
| P1-۸ نسخه‌های قبلی | دکمه «🗂 نسخه‌های قبلی» با دریافت PNG/PDF هر نسخه |
| P1-۹ docker-compose | `BOT_TOKEN` هم‌نام با بقیه |
| P1-۱۰ ترتیب middleware | `ErrorMiddleware` بیرونی‌ترین لایه شد |
| P1-۱۱ ثبت‌نام ناخواسته | فقط دعوت‌شده‌ها و ادمین‌های `ADMIN_IDS` در دیتابیس ثبت می‌شوند |
| افزوده | دستور `/id` برای دیدن شناسه تلگرام |

**تست‌ها: ۲۰۷ → ۲۳۶** (۲۹ تست جدید فقط برای همین تغییرات).

## آنچه هنوز باقی است (P2 — عمداً)

فرم مرحله‌ای per-field، کپی تک‌اسلات، ویرایش تک‌تکلیف، پنل ادمین داخل ربات،
Webhook، فاز ۳ (AI Planner / Analytics)، و ابزارهای کیفیت (ruff/mypy/Sentry).
فیلد `users.phone` و `activities.notes` هنوز بلااستفاده‌اند (`grade` اکنون استفاده می‌شود).


---

# نسخه نهایی — v1.0.0-final (مأموریت Full Bug Fix + Production Hardening)

## آنچه در این مرحله انجام شد

| بخش مأموریت | نتیجه |
|---|---|
| مالکیت عملیاتی دانش‌آموز توسط مشاور | ✅ افزودن/ویرایش/حذف/اتصال، کاملاً داخل ربات |
| کارت دانش‌آموز کامل | ✅ برنامه این هفته · برنامه جدید · برنامه‌های قبلی · ویرایش اطلاعات · اتصال به تلگرام · حذف |
| آیدی تلگرام اختیاری هنگام ساخت | ✅ `نام \| پایه \| 123456789` (فیلد سوم اختیاری) |
| لینک دعوت | ✅ ۳۲ نویسه CSPRNG · یک‌بارمصرف · انقضای ۱۴ روزه · قابل ابطال · قابل تمدید · Audit |
| جلوگیری از رکورد تکراری | ✅ ادغام رکورد سرگردان + رد لینک برای حساب متصل + عدم ثبت ناشناس‌ها |
| Timezone | ✅ `Asia/Tehran` در همه محاسبات + تست مرز نیمه‌شب |
| صفحه‌بندی | ✅ `ListCB` برای history/drafts/mine/student |
| Retention فایل | ✅ حذف برنامه = حذف فایل‌ها (فقط داخل STORAGE_ROOT) + `manage cleanup` |
| نسخه‌های قبلی | ✅ فهرست نسخه‌ها با دریافت PNG/PDF هر نسخه + کنترل دسترسی |
| Dead code | ✅ ۴ رشته و ۵ import بلااستفاده حذف شد؛ صفر TODO/FIXME |
| Handler/Button audit | ✅ تست خودکار: هر دکمه هندلر دارد، هیچ هندلری بی‌مصرف نیست، callback ≤ ۶۴ بایت |
| Authorization | ✅ IDOR روی student/plan/version/file/token — همه رد می‌شوند |
| Railway/Telegram audit | ✅ ۲۹ بررسی خودکار، همه سبز |

## باگ‌هایی که در همین مرحله کشف و رفع شدند

1. **گارد مسیر فایل** با `settings` سراسری چک می‌شد نه ریشه واقعی سرویس →
   در تنظیمات غیرپیش‌فرض، دانلود نسخه‌ها شکست می‌خورد.
2. **Cooldown مشترک رندر**: فشردن «پیش‌نمایش» و بلافاصله «تولید» باعث می‌شد
   تولید بی‌صدا بلاک شود. حالا cooldown به‌ازای هر اکشن است (دابل‌کلیک همان دکمه
   همچنان مهار می‌شود).
3. **قید بی‌نام در migration** خودکار (FK بدون نام) که روی SQLite/downgrade می‌شکست.

## ممیزی schema

| فیلد | وضعیت | تصمیم |
|---|---|---|
| `users.grade` | استفاده می‌شود (کارت و فهرست) | نگه‌داشته شد |
| `users.phone` | استفاده نمی‌شود | **رزرو مستندشده** برای ارسال پیامکی برنامه |
| `activities.notes` | استفاده نمی‌شود | **رزرو مستندشده** برای یادداشت خصوصی مشاور |
| `PlanStatus.READY` | استفاده نمی‌شود | **رزرو** برای وضعیت «آماده تولید» در آینده |
| `PlanStatus.ARCHIVED` | استفاده نمی‌شود | **رزرو** برای بایگانی به‌جای حذف |
| `users.invite_token/expires/issued` | جدید | چرخه کامل دعوت |
| `plan_files.*` | استفاده می‌شود | فهرست نسخه‌ها |

هیچ فیلدی حذف نشد (داده‌ای از بین نمی‌رود)؛ رزروها در تست
`test_database_fields_are_either_used_or_documented` مستند و قفل شده‌اند.

## تست‌ها

```
{'tests/test_audit.py': 13, 'tests/test_bot_flow.py': 13, 'tests/test_deployment.py': 19, 'tests/test_renderer.py': 26, 'tests/test_security.py': 36, 'tests/test_students.py': 55, 'tests/test_ux.py': 116}
مجموع: 278
```

## محدودیت‌های باقی‌مانده (واقعی)

* **Webhook** پیاده نشده؛ Long Polling با تک‌اینستنس (نیازمند دامنه است).
* **فرم مرحله‌ای per-field برای اسلات** وجود ندارد؛ فقط ورود سریع `درس | مبحث | کار | زمان`.
* **کپی تک‌اسلات** و **ویرایش تک‌تکلیف** نیست (کپی روز و جایگزینی کل تکالیف هست).
* **پنل ادمین داخل ربات** نیست؛ عملیات ادمینی از CLI انجام می‌شود.
* **Retention خودکار زمان‌بندی‌شده** نیست؛ `manage cleanup` باید دستی/کرون اجرا شود.
* **Sentry/alerting** نصب نشده (لاگ ساختاریافته و audit log هست).
* **ruff/mypy** در CI نیست.
* بخش تکالیف در قالب v2 سه خط نوشتاری دارد (باندهای اندازه‌گیری‌شده ۲۶/۲۴/۲۲ پیکسل)؛ فراتر از ظرفیت، خطای overflow با پیام و دکمهٔ ویرایش به مشاور داده می‌شود.
* فاز ۳ (AI Planner / Analytics) عمداً پیاده نشده است.


---

# v1.0.0-final2 — Security Hardening + Admin Control Panel

## باگ امنیتی رفع‌شده (بحرانی)

**Invite Role Hijack.** قبل از این نسخه، اگر مشاور روی لینک دعوت دانش‌آموز خودش
می‌زد، سیستم `telegram_id` او را به رکورد دانش‌آموز منتقل می‌کرد و **حساب مشاور
را غیرفعال و بی‌آیدی** می‌کرد (تأییدشده با PoC: `advisor tg=None active=False`).
عملاً مشاور از سیستم قفل می‌شد و در ورود بعدی، دانش‌آموز محسوب می‌شد.

**اصلاح:** ماژول جدید `app/services/invites.py` با `InviteOutcome`؛ بررسی نقش
**قبل از هر نوشتنی** انجام می‌شود. علاوه بر آن، لایه ریپازیتوری هم سخت‌گیر شد:
`claim_invite` فقط اجازه دارد یک رکورد **دانش‌آموز** بلااستفاده را ادغام کند و
برای هر نقش دیگری `PermissionError` می‌دهد (دفاع در عمق).

| قانون | تست |
|---|---|
| Advisor → لینک دانش‌آموز | `test_advisor_opening_student_invite_keeps_everything_intact` |
| Admin → لینک دانش‌آموز | `test_admin_opening_student_invite_stays_admin` |
| ADMIN_IDS بدون نقش دیتابیس | `test_admin_by_env_only_is_still_protected` |
| Student → لینک دیگری | `test_student_cannot_hijack_another_students_invite` |
| هیچ تنزل نقشی | `test_no_flow_can_downgrade_a_role` |

## پنل مدیریت

`app/bot/handlers/admin.py` + `app/services/admin_service.py` + `app/security.py`.
ده صفحه: مشاوران، دانش‌آموزان، وضعیت سیستم، وضعیت ربات، دیتابیس، Storage،
آمار، Audit Logs، تنظیمات و منوی اصلی. تمام اعداد از کوئری واقعی یا probe زنده
می‌آیند (تست `test_database_screen_uses_real_counts` مقادیر را با داده واقعی
مقایسه می‌کند). عملیات خطرناک (تعلیق، پاک‌سازی) تأیید دومرحله‌ای دارند و حساب
مدیر قابل تعلیق نیست.

## باگ‌های جانبی که همین مرحله پیدا شد

1. `_require_active` در لایه هندلر تعریف شده بود ولی هرگز صدا زده نمی‌شد →
   حذف شد و تعلیق فقط در لایه سرویس (`ensure_active`) اعمال می‌شود؛ تست جدید
   `test_no_unused_private_helpers` جلوی تکرار این دسته را می‌گیرد.
2. روتر جدید ادمین باعث خطای «Router is already attached» در تست‌ها شد →
   `tests/conftest.py` با fixture خودکار، تکرار در چهار فایل را هم حذف کرد.

## به‌روزرسانی وضعیت schema

`users.is_active` که قبلاً «رزرو» بود، حالا **استفاده می‌شود** (تعلیق حساب).
`phone`, `activities.notes`, `PlanStatus.READY/ARCHIVED` همچنان رزرو مستندند.

---

# ممیزی عمیق رندرر + بازطراحی چیدمان تکالیف (نسخه ۱.۳.۱)

## ریشهٔ باگ گزارش‌شده

بخش تکالیف در قالب v2 با مختصات **حدسی** ثبت شده بود:
`x=285 y=930 w=1150 h=62` → لبهٔ راست متن روی `x=1435` می‌افتاد، در حالی که
اندازه‌گیری پیکسلی نشان داد پنل چاپ‌شده فقط تا `x=1267` ادامه دارد. یعنی متن
تکالیف **۱۶۸ پیکسل بیرون از کادر** و روی موج تزئینی گوشهٔ پایین راست نوشته
می‌شد. علاوه بر آن:

* قرارگیری عمودی با فرمول جادویی `y = top + (band - size*1.15)/2` و لنگر
  `anchor="ra"` (ascender) انجام می‌شد؛ هیچ ارتباطی با baseline واقعی فونت
  نداشت، پس خط سوم از کف کادر بیرون می‌زد.
* بک‌اند HTML اندازهٔ فونت را **داخل مرورگر** دوباره محاسبه می‌کرد
  (`FIT_SCRIPT`)، بنابراین پیش‌نمایش و خروجی نهایی می‌توانستند واگرا شوند.
* چیپ چاپ‌شدهٔ «تکالیف» (که تا `y=936` پایین می‌آید) در هیچ محاسبه‌ای دیده
  نمی‌شد و متن می‌توانست از زیر آن رد شود.

## هندسهٔ اندازه‌گیری‌شده (از خود تصویر، نه از پرامپت)

| ناحیه | مختصات |
|---|---|
| Outer panel | x 268 · y 916 · w 1000 · h 83 (تا 1268×999) |
| Border | top 917 · bottom 997 · left 269 · right 1267 |
| Title chip (مانع) | x 650 · y 884 · w 234 · h 53 |
| Body (ناحیهٔ نوشتاری) | x 287 · y 919 · w 961 · h 78 |
| خط نقطه‌چین ۱ | y 946 (ضخامت ۲) · x 287→1247 |
| خط نقطه‌چین ۲ | y 973 (ضخامت ۲) |
| باند ۱ | x 287 y 919 w 961 h 26 · عرض قابل استفاده **358** (چیپ) |
| باند ۲ | x 287 y 948 w 961 h 24 · عرض قابل استفاده 961 |
| باند ۳ | x 287 y 975 w 961 h 22 · عرض قابل استفاده 961 |
| baselineها | 938 · 965 · 990 (در سایز ۱۸) |
| فونت | Vazirmatn Medium · max 18 · **min 13** |
| max_lines | ۳ |
| safe margin | 4/4/2/2 px + baseline_gap = 1px بالای هر خط |

## مدل جدید

```
Plan → normalize → wrap → fit → baseline → TextLine → (Pillow | Chromium)
```

* `app/rendering/compose.py` تنها جایی است که مختصات تولید می‌شود؛ هر دو بک‌اند
  همان لیست `TextLine` را می‌کشند. پیش‌نمایش = خروجی نهایی، PNG = PDF،
  Pillow = Chromium (اختلاف اندازه‌گیری‌شده ≤ ۱ پیکسل).
* هر خط روی **baseline واقعی** می‌نشیند: `baseline = band.bottom − ink.bottom −
  baseline_gap`. `ink` از یک رشتهٔ مرجع ثابت (بلندترین صعودها و نزول‌ها) گرفته
  می‌شود، پس ریتم خطوط ثابت می‌ماند و جوهر هرگز از باند بیرون نمی‌زند.
* باندی که با هنر چاپ‌شده تلاقی دارد (`obstacles` در JSON) عرض کمتری دارد؛ اگر
  متن آنجا جا نشود، الگوریتم آن را به باند بعدی می‌برد به‌جای اینکه روی چیپ
  بنویسد.
* چیدمان ترجیحی «یک تکلیف در هر خط» است؛ اگر تعداد بیشتر از باندها باشد به
  حالت جریانی با جداکنندهٔ «—» برمی‌گردد و کمترین تعداد خط را انتخاب می‌کند.
* سه وضعیت `FIT / TIGHT / OVERFLOW`؛ فقط `OVERFLOW` جلوی تولید را می‌گیرد و به
  مشاور پیام «⚠️ تکالیف بیش از ظرفیت قالب است» با دکمهٔ «✏️ ویرایش تکالیف»
  نشان می‌دهد.
* کش حالا `layout.fingerprint` (هش کل JSON هندسه) را هم در نظر می‌گیرد، پس
  تغییر یک مختصات هم کش را باطل می‌کند.

## ابزارها

* `python -m tools.calibrate assignment` — اورلی دیباگ: outer/title/body/باندها،
  baselineها و جعبهٔ جوهر واقعی.
* `python -m tools.ink_audit` — تفاضل خروجی با قالب دست‌نخورده و انتساب هر
  پیکسل جوهر به کادر خودش.
* `python -m tools.goldens` — بازتولید Golden Imageها (فقط با تغییر عمدی قالب).

## نتیجهٔ عددی

هیچ پیکسل جوهری خارج از کادرهای اعلام‌شده در هیچ‌کدام از سناریوها
(خالی، کوتاه، بلند، چندخطی، فارسی/انگلیسی، اموجی، نویسهٔ خاص، ۵۶ سلول پر،
سرریز عمدی) — **stray = 0**.
